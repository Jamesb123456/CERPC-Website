# Example environment configuration
# Copy to .env and adjust as needed

# Port for the optional Express static server (server.js)
PORT=3000

# React app variables must be prefixed with REACT_APP_
# Example (uncomment and set if you implement API calls in JS):
# REACT_APP_API_BASE_URL=https://api.example.com

# Note: The Google Analytics measurement ID is embedded in public/index.html.
# GA Measurement IDs are safe to be public and not considered secrets.
